[package]
name = "memory_address_calculator"
version = "1.0.0"
edition = "2021"
authors = ["Your Company"]
description = "Memory address converter from reference (ROM) space to working (RAM) space"
license = "MIT"

[dependencies]
# No external dependencies - uses only Rust standard library

[profile.release]
opt-level = 3
lto = true
